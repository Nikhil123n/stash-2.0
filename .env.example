DISCORD_TOKEN=
STASH_OWNER_ID=
STASH_ENV=production
GOOGLE_APPLICATION_CREDENTIALS=./gcp-key.json
GCP_PROJECT_ID=
GCP_LOCATION=us-central1
GROQ_API_KEY=
SANDBOX_FILE=./sandbox_data.json
FFMPEG_LOCATION=           # optional: path to ffmpeg/ffprobe binaries if not on PATH

# mymind auth — cookie-based, zero credits
# For Railway deployment (from scripts/export_cookies.py):
MYMIND_JWT=
MYMIND_CID=
MYMIND_AUTHENTICITY_TOKEN=
# For local Windows run: leave above empty, bot uses keyring automatically
